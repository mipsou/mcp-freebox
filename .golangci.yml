version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused
    - misspell
    - gofmt
    - gosimple
  settings:
    govet:
      enable:
        - unsafeptr

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
